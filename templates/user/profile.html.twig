{% extends "base.html.twig" %}

{% block body %}
    {{ include('default/_navbar.html.twig') }}

    <h1>Mon profil</h1>

    <p>{{ user.firstName }}</p>
    <p>{{ user.lastName }}</p>
    <p>{{ user.email }}</p>

    <a href="{{ path('edit_profile') }}">Modifier</a>

{% endblock %}